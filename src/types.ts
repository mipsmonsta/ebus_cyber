export interface BusMileage {
  Month: string;
  BusModel: string;
  Mileage: number;
}

export interface MonthlyTotal {
  Month: string;
  TotalMileage: number;
}

export interface ModelTotal {
  BusModel: string;
  Mileage: number;
}
